package com.reserve.core.repository.user;

import com.reserve.core.entity.user.UserInflow;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserInflowRepository extends JpaRepository<UserInflow, Long> {
}
